spring:
  batch:
    job:
      enabled: false
  profiles:
    group:
      local:
        - data-local
        - cloud-local
      dev:
        - data-dev
        - cloud-dev
      prod:
        - data-prod
        - cloud-prod

  jackson:
    time-zone: Asia/Seoul
